FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer — only re-runs if package.json changes)
COPY package*.json ./
RUN npm install

# Copy source (overridden by volume mount in dev, but needed for image builds)
COPY . .

EXPOSE 4000

CMD ["npm", "run", "dev"]